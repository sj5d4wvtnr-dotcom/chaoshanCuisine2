[build]
  publish = "release/wwwroot"
  command = "curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel 10.0 && export PATH=\"$PATH:$HOME/.dotnet\" && dotnet publish -c Release -o release"